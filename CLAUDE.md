# TRINITY ONE 사이트 작업 규칙

이 저장소는 TRINITY ONE의 GitHub Pages 사이트입니다.
새 페이지를 만들거나 기존 페이지를 수정할 때 아래 규칙을 반드시 따르세요.

## 배포 구조

- GitHub Pages가 `main` 브랜치를 배포합니다. 작업 브랜치에 커밋·푸시한 뒤 PR을 만들어 **squash 머지**해야 사이트에 반영됩니다.
- 사이트 주소: `https://trinityone.co.kr/` (커스텀 도메인, 가비아 DNS → GitHub Pages. 구 주소 `https://hq1trinity-arch.github.io/trinity-lecture-page/`는 자동 리다이렉트)
  - URL 체계 (2026-08-25 재편):
    - 리쿠르팅(회사 소개) 사이트: 루트 `/` (`index.html`, 에셋·JS는 `trinity-one/` 디렉터리 참조)
    - 고객용 사이트: `/customer/`
    - 강의 페이지: `/class-날짜` (예: `class-0728.html`, `class-0728-busan.html`, `class-0730-seoul.html`, `class-0820.html`, `class-0820-b.html`, `class-0908/`, `class-0909-kim/`, `class-0909-kang/`)
    - 구 경로(`/trinity-one/`, `/insurance/`, `/busan.html`, `/busan-0908/` 등)에는 **리다이렉트 스텁**이 있다. 페이지를 옮기거나 이름을 바꿀 때는 반드시 구 경로에 스텁(meta refresh + JS redirect + OG 유지)을 남긴다.
- 머지 후 "pages build and deployment" 워크플로가 성공해야 실제 반영됩니다. 배포 성공까지 확인하고 보고하세요.
- 페이지를 수정하면 로컬 서버 + Chromium(Playwright, `/opt/pw-browsers/chromium`)으로 **데스크톱(1280px)과 모바일(390px) 양쪽을 렌더링해 스크린샷으로 검증**한 뒤 커밋하세요.

## 브랜드 아이덴티티 (CI)

- 컬러 시스템 — **웹사이트는 퍼플 팔레트를 쓴다.** 새 웹페이지도 아래 웹 팔레트를 기준으로 만든다.
  - 웹 팔레트 (현재 `/trinity-one/` 적용 기준, basecamp.html 계열로 통일)
    - Violet `#4A1BD0` — 주색 (버튼, 강조 텍스트, 수치)
    - Violet Dark `#2E0F9E` — 호버·프레스
    - Lavender `#6E4AE8` — 카드 라벨(THREE PILLARS 등), 보조 강조
    - Light Lavender `#A78BFF` — 다크 배경 위 태그라인
    - Ink `#160E2E` — 텍스트 (본문 반투명은 `rgba(22,14,46,...)`)
    - Lavender Tint `#F2EFFE` — 밝은 배경
    - Border `#E3DEF3` — 카드 테두리·구분선
    - Deep Purple `#0B0320` → `#260C6B` — 다크 섹션 그라디언트
  - 브랜드 가이드(PPT) 원본 CI — 인쇄물·PT 등 오프라인 자료용으로만 사용
    - Bronze `#8B5E3C` / Gold `#C8A96E` / Charcoal `#2B2522` / Off-White `#F7F4EE` / Gray `#8E8880`
  - 웹에서 갈색·금색 계열을 새로 쓰지 않는다 (`--trinity-brown` 계열 토큰도 퍼플로 정리 완료)
- 서체: 국문 본문 Pretendard (`assets/fonts/`에 9종 웨이트 보유, 재다운로드 금지)
- 태그라인: **"The Only Strategy for the 0.1%"** — 히어로 배지·다크 섹션·푸터에 사용
- 브랜드 철학: 3×3×3 = ONE — 세 기둥(사람·돈·지식) × 세 전문 영역(법률·세무·금융) × 세 가치(보호·증식·영속)
- 로고: `assets/trinity-one-logo.png` — **색상 변경 절대 금지** (비율 왜곡·회전도 금지, 주변 여백 확보)
  - 퍼플 배경 위에서도 원본 브론즈 로고를 그대로 쓴다. CSS 필터로 색을 바꾸는 것도 금지.
  - 브랜드 가이드가 인정하는 변형은 Bronze on Light / Reverse on Dark(흰색) 두 가지뿐이다.
- 연락처 이메일: hq1.trinity@gmail.com (자체 도메인 확보 전 임시. trinityone.com은 타인 소유이므로 절대 사용 금지)
- 대표 유선번호: 02-2138-0797 (`tel:0221380797`)
- 주소: 서울특별시 강남구 테헤란로77길 11-15 12F (basecamp 계열 강의장과 동일 건물)
- 카카오톡 상담·커피챗 링크: http://pf.kakao.com/_RilxnX/chat (CTA 버튼은 채널 홈이 아닌 이 채팅 링크로 연결)
- 후원: 인카금융서비스(주) — 2022 코스닥 상장 GA, 22,637명+ 설계사, 0.00% 불완전판매율

## 링크 미리보기(OG) 규칙

- `og:image`는 **1200×630** 고정. 로고를 중앙에 두고 상하좌우 여백을 넉넉히 넣어 어떤 크롭에서도 로고가 잘리지 않게 만든다 (흰 배경에 로고 높이 약 360px).
- 미리보기 이미지를 교체할 때는 **파일명을 바꿔서**(예: `-v2`, `-v3`) 카카오톡 캐시를 회피한다.
- 파비콘은 원본 로고 PNG를 그대로 사용한다.
- 미리보기 제목·설명에 "보험 기반" 문구를 쓰지 않는다.
- 교체 후 사용자에게 카카오 공유 디버거(https://developers.kakao.com/tool/debugger/sharing)에서 캐시 초기화가 필요할 수 있음을 안내한다.

## 사진 규칙

- 사진은 `.pband` 밴드(섹션 사이 배너)와 `#about` 배경으로만 쓴다. 3~4장이 상한.
- **듀오톤은 CSS로 처리**한다 (`filter: grayscale(1) ... brightness(0.52)` + 퍼플 오버레이).
  원본 파일은 컬러 그대로 두므로, 같은 파일명으로 교체만 하면 톤이 자동 유지된다.
- 인물 사진은 **얼굴이 나오지 않는 컷**을 우선한다 (손·서류·공간·야경). 서구권 모델 이슈와 스톡 티를 동시에 피할 수 있다.
- **강의·행사 사진은 강사와 참석자 얼굴을 반드시 블러 처리**한다 (타원 마스크 + 가장자리 페더로 자연스럽게).
- 현재 사용 중: `photo-quote.jpg`(서울 야경·인용구 배경) / `photo-services.jpg`(손·서류) / `photo-process.jpg`(서명) / `photo-lecture.jpg`(강의 현장, 얼굴 블러 처리본)
- **사진 밴드는 서로 붙이지 않는다.** 다크 섹션(`#about`) 바로 뒤에 배너를 두면 사진이 겹쳐 보인다. 사이에 본문 섹션을 두고 최소 1,000px 이상 간격을 확보한다.

## 수치 표기 규칙

- TRINITY ONE은 신생 조직이므로 **자체 실적 수치를 공개 페이지에 쓰지 않는다** (누적 컨설팅 건수, FA 정착률 등).
  - 예외: **700건+ 누적 공동 컨설팅**은 사용자가 명시 승인(2026-08-24)해 파트너십 섹션 Proof Point로 사용 중.
- **성과 배분 비율(50:50 등)은 민감한 내부 정보**이므로 노출 금지.
- FA 소득·판매 실적 수치도 금지 (과장 광고로 읽힐 수 있음). 사업설명회 자리에서 직접 설명할 내용이다.
- 사용 가능한 수치: 인카금융서비스의 공개 실적(매출·설계사 수·상장 연도), 공신력 있는 기관의 시장 통계(출처 명기 필수).

## 모바일 줄바꿈 규칙 (가장 중요 — 사용자가 반복 강조)

- **모든 문장은 문장·구(句) 경계에서만 줄바꿈**한다. 조사·명사구 중간에서 끊기면 안 된다.
  - 나쁜 예: "보험이라는 / 도구로", "정관·퇴직금 / 규정", "기업의 / 리스크를"
  - 좋은 예: 마침표·쉼표 뒤, 또는 의미 단위("~하고", "~해", "등") 뒤에서 줄바꿈
- 구현 패턴 (trinity-one/index.html에 이미 적용됨):
  - `<br>` — 모든 화면에서 줄바꿈 (문장 경계)
  - `<br class="m-only">` — 모바일(≤860px)에서만 줄바꿈
  - `<br class="d-only">` — 데스크톱에서만 줄바꿈
  - `body`에 `word-break: keep-all` 유지
- 요청한 줄이 한 줄에 들어가지 않으면 해당 문단만 폰트를 소폭 줄여서(`.m-fit` 참고) 맞춘다.
- **페이지를 수정한 뒤에는 모바일(390px) 전체를 스크린샷으로 훑어** 어색하게 끊긴 줄이 없는지 반드시 재점검한다.
- 제목·CTA 등 가운데 정렬 요청이 있었던 요소는 유지한다 (모바일 히어로 제목, 상담 카드 제목은 가운데 정렬).

## 기타

- 기존 강의 페이지(`class-*` 경로)를 새 작업으로 덮어쓰지 않는다. 새 페이지는 별도 경로로 만든다. 루트 `index.html`은 리쿠르팅 사이트다.
- 리쿠르팅 사이트(루트 `index.html`)는 React 런타임(`trinity-one/dc-runtime.js`)을 로컬 파일로 포함하고 있다. CDN(unpkg) 의존을 새로 추가하지 않는다.
- 커밋 메시지는 한국어로, 변경 내용을 구체적으로 쓴다.
